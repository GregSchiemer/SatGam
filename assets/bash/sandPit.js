

function makeHenge(ctx, spec){let henge={
		    N: 25,
	   phoneW: 30,
	   phoneH: 56,
    keyRadius: 18,
arcRadiusMode: 'feet',
       swivel: function(){
const arcRadius = (phoneW / (2 * Math.tan(Math.PI / N)))
//		);	
//	};
//	henge.swivel();
//	return henge;
}

/*
	const N = 25;
	const phoneW = 30; //40;
	const phoneH = 56; //70;
	const keyRadius = 18; // can be whatever; it doesn't affect foot-touch
	
	const arcRadius = (phoneW / (2 * Math.tan(Math.PI / N))); // feet-touch

	const { slots, ctxS } = makeHenge(ctx, {
	  N,
	  arcRadiusMode: 'feet',
	  phoneW,
	  phoneH,
	  arcRadius,
	  keyRadius,
	});
*/